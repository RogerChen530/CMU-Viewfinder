-- ============================================================
-- 相簿補上「作者」跟「創作理念」欄位，供燈箱詳細頁使用。
--
-- author 是自由文字（不綁定 uploaded_by），因為實際拍攝者
-- 不一定是後台上傳照片的那個管理員，讓 admin 可以自己填掛名。
-- ============================================================

alter table photos
  add column author text,
  add column description text;
