-- ============================================================
-- 註冊時新增「學生／外部人士」身份選擇：
-- - 學生：跟原本一樣填學號，管理員照學號審核
-- - 外部人士：不用填學號，審核方式由社團之後另外決議，
--   目前先讓 admin 在後台看得出這是外部人士申請即可
--
-- unique constraint 不受影響：Postgres 的 unique 允許多個 NULL
-- 同時存在（NULL 互相不視為相等），所以多個外部人士都留空
-- student_id 不會撞到 unique 規則。
-- ============================================================

alter table profiles
  alter column student_id drop not null;
